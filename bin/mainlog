#!/bin/sh

if [ -n "${1:-}" ]; then
  path="$HOME/arma3/$1"
  shift
else
  path="$HOME/appdata"
fi
less +F "$(\ls $path/arma3server_* | tail -n1)" $@
#less +F "$(\ls ~/Arma\ 3/arma3server_* | tail -n1)" $@
